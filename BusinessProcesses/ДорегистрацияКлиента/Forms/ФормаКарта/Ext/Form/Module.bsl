
&НаСервере
Процедура ОбновитьНаСервере() 
	БизнесПроцессТекущий = БизнесПроцесс.ПолучитьОбъект();
	Карта = БизнесПроцессТекущий.ПолучитьКартуМаршрута();	
КонецПроцедуры


&НаКлиенте
Процедура Обновить(Команда)
	ОбновитьНаСервере();
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	ОбновитьНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	БизнесПроцесс = Параметры.БизнесПроцесс; 
КонецПроцедуры;
